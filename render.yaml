services:
  - type: web
    name: mundo-running-backend
    rootDir: backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 8080
      - key: MONGO_URI
        sync: false
      - key: KEY_ADMIN
        sync: false